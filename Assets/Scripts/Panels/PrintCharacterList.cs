using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PrintCharacterList : MonoBehaviour
{
    /*public GameTextDisplay gameTextDisplay;

    void Start()
    {
        // Find the GameTextDisplay object in the scene and assign it to the gameTextDisplay variable
        GameObject gameTextDisplayObject = GameObject.Find("ContentText");

        gameTextDisplay = gameTextDisplayObject.GetComponent<GameTextDisplay>();

        // Get the Button component attached to this game object
        Button button = GetComponent<Button>();

        // Add an onClick event to the button that calls the DisplayCharacterList method in the GameTextDisplay script
        button.onClick.AddListener(gameTextDisplay.DisplayCharacterList);
    }*/
}
